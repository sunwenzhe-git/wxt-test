/*
 *  Author: swz
 *  Description:
 */
import { createTheme } from "@mantine/core";

export default createTheme({
  /** Put your mantine theme override here */
  fontSizes: {
    xs: "11px",
    sm: "12px",
    md: "14px", // 默认字体大小
    lg: "16px",
    xl: "20px",
  },
});
